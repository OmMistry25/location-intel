[project]
name = "intelligence"
version = "0.0.0"
description = "Derived metrics and composite scores for location-intel."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/intelligence"]
